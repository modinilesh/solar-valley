# Optional server-side email relay settings.
# Copy these names into the environment settings of the serverless/backend host.
# Never place real values in files under docs/ or commit a populated .env file.
MAIL_HOST=smtpout.secureserver.net
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=
